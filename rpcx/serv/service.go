package main

import (
	"flag"
)

var (
	addr = flag.String("addr", "localhost:9000", "server address")
)

func main() {
	//flag.Parse()
	//s := server.Server{}
	//s.RegisterName("Arith", new(example.Arith), "")
	//go s.Serve("tcp", *addr)
	//select {}
}